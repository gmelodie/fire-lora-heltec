# Copy to .env on the public server. Without it, compose serves the self-signed
# cert.pem/key.pem on port 8443 only, which is what a laptop wants.

COMPOSE_FILE=docker-compose.yml:docker-compose.prod.yml
DOMAIN=cerrado.gmelodie.com
LETSENCRYPT_EMAIL=you@example.com
